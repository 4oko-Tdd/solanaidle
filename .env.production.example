NODE_ENV=production
PORT=3000

# JWT signing secret — use a long random string
JWT_SECRET=change_me_to_a_long_random_string

# Server keypair for signing ER transactions (base58-encoded private key bytes)
SERVER_KEYPAIR=

# Allowed CORS origins (comma-separated, or * for all)
CORS_ORIGIN=https://yourdomain.com

# Solana RPC endpoint
RPC_URL=https://api.devnet.solana.com

# MagicBlock ER validator URL
ER_VALIDATOR_URL=https://devnet-us.magicblock.app
